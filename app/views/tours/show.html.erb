<p id="notice"><%= notice %></p>

<p>
  <strong>Agent:</strong>
  <%= if @tour.agent %>
  <%= link_to "#{@tour.agent.name}", agent_path(@sale.agent) %>
  <% end %>
</p>

<p>
  <strong>Client:</strong>
  <%= if @our.client %>
  <%= link_to "#{@tour.client.name}", client_path(@tour.client) %>
  <% end %>
</p>

<p>
  <strong>Date:</strong>
  <%= @tour.date %>
</p>

<p>
  <strong>Room:</strong>
  <%= if @tour.room %>
  <%= link_to "#{@tour.room.room_number}", room_path(@tour.room) %>
  <% end %>
</p>

<p>
  <strong>Property:</strong>
  <%= if @tour.property %>
  <%= link_to "#{@tour.property.address}", property_path(@tour.property) %>
  <% end %>
</p>

<p>
  <strong>Comments:</strong>
  <%= @tour.comments %>
</p>

<%= link_to 'Edit', edit_tour_path(@tour) %> |
<%= link_to 'List all tours', tours_path %>
