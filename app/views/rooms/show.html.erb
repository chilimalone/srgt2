<p id="notice"><%= notice %></p>

<p>
  <strong>Property:</strong>
  <% if @room.property %>
  <%= link_to "#{@room.property.address}", property_path(@room.property) %>
  <% end %>
</p>

<p>
    <strong>Owner: </strong>
    <% if @room.individual %>
    <%= link_to "#{@room.individual.name}", individual_path(@room.individual) %>
    <% end %>
</p>

<p>
  <strong>Street 2/Unit:</strong>
  <%= @room.room_number %>
</p>

<p>
  <strong>Comments:</strong>
  <%= @room.comments %>
</p>

<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'List all rooms', rooms_path %>
